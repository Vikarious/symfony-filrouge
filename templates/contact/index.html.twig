{% extends 'base.html.twig' %}

{% block body %}
    <style>
        .wrapperF {
            margin: 1em auto;
            max-width: 800px;
            width: 95%;
            font: 18px / 1.5 sans-serif;
        }
    </style>

    <div class="wrapperF">
        <h1>Share with us some feedbacks !</h1>
        {# {{ dump () }} #}
        {{ form(form) }}
    </div>
    {% endblock %}
